export const hasNaNOrUndefined = (value) => {
    if (value === undefined) {
        return true;
    }
    return Number.isNaN(value);
};
export const isDefined = (value) => {
    return value !== undefined;
};
export const hasItems = (array, minItemCount = 1) => Array.isArray(array) && array.length >= minItemCount;
//# sourceMappingURL=index.js.map