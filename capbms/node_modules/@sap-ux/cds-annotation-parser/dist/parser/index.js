import { lexer, parser } from './factory.js';
export const parse = (text) => {
    const { tokens, errors: lexErrors } = lexer.tokenize(text);
    parser.text = text;
    parser.input = tokens;
    return {
        cst: parser.declaration(),
        tokens,
        lexErrors,
        parseErrors: parser.errors
    };
};
//# sourceMappingURL=index.js.map