/**
 * @file Detect usage of session storage
 */
import { createStorageRuleHelpers } from '../utils/helpers.js';
// ------------------------------------------------------------------------------
// Rule Definition
// ------------------------------------------------------------------------------
const rule = {
    meta: {
        type: 'problem',
        docs: {
            description: 'fiori tools (fiori custom) ESLint rule',
            recommended: false
        },
        messages: {
            sessionStorageUsage: 'For security reasons, the usage of session storage is not allowed in a Fiori application'
        },
        schema: []
    },
    create(context) {
        const FORBIDDEN_STORAGE_OBJECT = [];
        const storageHelpers = createStorageRuleHelpers('sessionStorage', 'sessionStorageUsage');
        return {
            'VariableDeclarator'(node) {
                storageHelpers.processVariableDeclarator(node, FORBIDDEN_STORAGE_OBJECT);
            },
            'MemberExpression'(node) {
                storageHelpers.checkMemberExpression(node, FORBIDDEN_STORAGE_OBJECT, context);
            }
        };
    }
};
export default rule;
//# sourceMappingURL=sap-no-sessionstorage.js.map