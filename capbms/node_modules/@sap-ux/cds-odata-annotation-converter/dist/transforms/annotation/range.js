import { Position, Range } from '@sap-ux/odata-annotation-core';
export const copyPosition = (position) => Position.create(position.line, position.character);
export const copyRange = (range) => range ? Range.create(copyPosition(range.start), copyPosition(range.end)) : undefined;
export const createRange = (start, end) => start && end ? Range.create(copyPosition(start), copyPosition(end)) : undefined;
//# sourceMappingURL=range.js.map