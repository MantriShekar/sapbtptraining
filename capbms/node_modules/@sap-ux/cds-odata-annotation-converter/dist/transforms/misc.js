import { join } from 'node:path';
import { pathToFileURL } from 'node:url';
export const toAbsoluteUriString = (root, relativeUri) => {
    return pathToFileURL(join(root, relativeUri)).toString();
};
//# sourceMappingURL=misc.js.map